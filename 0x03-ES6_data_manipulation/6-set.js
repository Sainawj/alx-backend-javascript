/**
 * Converts an array into a Set.
 * @param {Array} array - The input array to be converted into a Set.
 * @returns {Set} - A Set containing the unique elements of the input array.
 */
export default function convertArrayToSet(arr) {
    return new Set(arr);
  }
  